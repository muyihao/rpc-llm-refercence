# AI Record Processor Project

This project implements a custom Processor for Ray Data using RPC to external inference actors.
It includes fault tolerance for dynamic service changes and performance optimizations like caching and backoff.

## Setup
- Install Ray: `pip install ray`
- Other deps: pydantic, etc.

## Usage
Run `python examples/usage.py`